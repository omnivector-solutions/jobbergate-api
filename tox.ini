[tox]
envlist = lint
skipsdist = True

[testenv]
deps = -r{toxinidir}/requirements/test-requirements.txt
setenv = VIRTUAL_ENV={envdir}
         PYTHONHASHSEED=0
         TERM=linux
passenv = http_proxy https_proxy

[testenv:lint]
commands = flake8 --ignore=E501 {posargs} jobbergate_api/ apps/
